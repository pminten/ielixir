defmodule IElixir.Helpers do
  @moduledoc """
  Helpers for within IElixir.
  """
  # TODO: Actually define some helpers. Currently this module just serves as a
  # placeholder that's used in a call to `:elixir.eval` to get a scope.
end
